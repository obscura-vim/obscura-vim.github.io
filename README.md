Small landing page for ObscuraVim. Static, fast, dependency-free.

Local run:

```bash
python -m http.server 8000
```

Open `http://localhost:8000`.

Files: `index.html`, `style.css`, `script.js`, `demo.mp4`.

Deploy: GitHub Pages.
